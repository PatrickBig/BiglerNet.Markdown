@page "/"

@if (input != null)
{
    <EditForm Model="input">
        <MarkdownEditor @bind-Value="input" />
        <MarkdownViewer Content="@input" />
    </EditForm>
}

@code {
    string input = "# Sample header\r\nSome text\r\nA smile :)\r\nA sample [link](https://github.com)";
}